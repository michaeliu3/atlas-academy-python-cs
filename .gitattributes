# Markdown source maps intentionally use two trailing spaces for hard line breaks.
*.md whitespace=-trailing-space
# Wrangler emits a few declaration continuations with trailing spaces.
worker-configuration.d.ts whitespace=-trailing-space
